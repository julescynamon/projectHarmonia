---
interface Dimension {
  title: string;
  description: string;
  icon: string;
  backgroundImage: string;
  accentColor: string;
}

const dimensions: Dimension[] = [
  {
    title: "Dimension du Corps",
    description: "La Maison Sattvaïa prend soin du corps, humain comme animal, à travers la naturopathie et les approches naturelles. Alimentation, hygiène de vie, plantes et pratiques douces deviennent des alliés pour restaurer l'équilibre, renforcer la vitalité et soutenir un bien-être global. Un corps en harmonie est la première porte vers la paix intérieure.",
    icon: "healing",
    backgroundImage: "/images/dimensions-sante-naturelle.webp",
    accentColor: "sage"
  },
  {
    title: "Dimension de l'Énergie",
    description: "L'énergie est le souffle de vie. Par les soins énergétiques et chamaniques, La Maison Sattvaïa libère les blocages, réaccorde les vibrations et favorise la circulation fluide de l'énergie. Quand l'énergie circule librement, l'être retrouve paix, clarté et force intérieure.",
    icon: "self_improvement",
    backgroundImage: "/images/dimensions-rituels-energie.webp",
    accentColor: "gold"
  },
  {
    title: "Dimension de l'Âme",
    description: "Au-delà du corps et de l'énergie, La Maison Sattvaïa est un espace de guidance et de reconnexion spirituelle. Lectures d'âme, numérologie karmique et accompagnements personnalisés ouvrent des chemins de conscience et de transformation profonde. C'est une invitation à retrouver la lumière de son essence et à marcher aligné avec sa mission de vie.",
    icon: "psychology",
    backgroundImage: "/images/dimensions-developpement-spirituel.webp",
    accentColor: "eucalyptus"
  }
];
---

<section class="py-20 md:py-32 bg-gradient-to-b from-cream/50 to-white relative overflow-hidden">
  <!-- Éléments décoratifs de fond -->
  <div class="absolute inset-0 bg-nature-pattern opacity-3"></div>
  <div class="absolute top-10 left-10 w-64 h-64 bg-sage/5 rounded-full blur-3xl animate-pulse"></div>
  <div class="absolute bottom-10 right-10 w-80 h-80 bg-gold/5 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s;"></div>
  
  <div class="container mx-auto px-4 relative">
    <!-- En-tête éditorial -->
    <div class="text-center max-w-4xl mx-auto mb-20" data-aos="fade-up" data-aos-duration="1000">
      <div class="inline-block mb-8">
        <span class="bg-gradient-to-r from-sage to-gold bg-clip-text text-transparent font-slogan text-2xl md:text-3xl font-bold uppercase tracking-wider">
          Nos dimensions
        </span>
      </div>
      <p class="text-xl md:text-2xl text-ebony leading-relaxed font-slogan max-w-3xl mx-auto mb-8">
        Trois chemins d'accompagnement, une même essence : retrouver l'harmonie et révéler la lumière intérieure.
      </p>
      <div class="w-24 h-1 bg-gradient-to-r from-sage to-gold mx-auto mt-8 rounded-full"></div>
    </div>

    <!-- Layout Magazine - Cards horizontales -->
    <div class="space-y-8 lg:space-y-12">
      {dimensions.map((dimension, index) => (
        <div 
          class={`group relative overflow-hidden rounded-3xl shadow-2xl hover:shadow-3xl transition-all duration-700 ${
            index % 2 === 0 ? 'lg:flex-row' : 'lg:flex-row-reverse'
          }`}
          data-aos="fade-up" 
          data-aos-delay={index * 200}
          data-aos-duration="1000"
        >
          <div class={`flex flex-col lg:flex-row ${index % 2 === 0 ? '' : 'lg:flex-row-reverse'} min-h-[400px]`}>
            
            <!-- Section Image -->
            <div class="relative lg:w-1/2 h-64 md:h-80 lg:h-auto overflow-hidden">
              <!-- Image réelle -->
              <img
                src={dimension.backgroundImage}
                alt={`Image représentant ${dimension.title.toLowerCase()}`}
                class="absolute inset-0 w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
                loading="lazy"
              />
              

              
              <!-- Effet parallax sur hover -->
              <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
            </div>

            <!-- Section Contenu -->
            <div class={`lg:w-1/2 bg-white relative flex flex-col justify-center p-6 md:p-8 lg:p-10 xl:p-12`}>
              
              <!-- Accent décoratif -->
              <div class={`absolute top-0 ${index % 2 === 0 ? 'left-0' : 'right-0'} w-2 h-full bg-gradient-to-b ${
                dimension.accentColor === 'sage' ? 'from-sage to-eucalyptus' :
                dimension.accentColor === 'gold' ? 'from-gold to-sage' :
                'from-eucalyptus to-sage'
              }`}></div>
              
              <div class="relative z-10">
                <!-- Titre -->
                <h3 class="font-heading text-2xl md:text-3xl lg:text-4xl xl:text-5xl mb-4 md:mb-6 leading-tight bg-gradient-to-r from-sage to-gold bg-clip-text text-transparent">
                  {dimension.title}
                </h3>
                
                <!-- Description -->
                <p class="text-base md:text-lg lg:text-xl text-ebony leading-relaxed font-slogan">
                  {dimension.description}
                </p>
              </div>
              
              <!-- Effet de brillance -->
              <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 pointer-events-none">
                <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent transform -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-1000"></div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    
  </div>
</section>

<style>
  .bg-nature-pattern {
    background-image: 
      radial-gradient(circle at 25% 25%, rgba(139, 146, 134, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 75% 75%, rgba(139, 146, 134, 0.1) 0%, transparent 50%);
  }

  /* Effet de profondeur sur les cartes */
  .group:hover {
    transform: translateY(-8px);
  }
  
  /* Animation parallax subtile */
  @keyframes float-gentle {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-5px); }
  }
  
  .animate-float-gentle {
    animation: float-gentle 6s ease-in-out infinite;
  }
  
  /* Effet de glow sur hover */
  .shadow-3xl {
    box-shadow: 0 35px 60px -12px rgba(0, 0, 0, 0.25);
  }
</style>

---
import MainLayout from './MainLayout.astro';

interface Props {
  title: string;
  description: string;
  noindex?: boolean;
}

const { title, description, noindex = false } = Astro.props;
---

<MainLayout title={title} description={description} noindex={noindex}>
  <main>
    <slot />
  </main>
</MainLayout>
